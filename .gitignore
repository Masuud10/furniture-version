node_modules/
.next/
out/
build/
dist/
coverage/
.vercel/
*.tsbuildinfo
next-env.d.ts

.env
.env.local
.env*.local
!.env.example

supabase/.branches/
supabase/.temp/
supabase/.env

/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

.DS_Store
Thumbs.db
*.log
npm-debug.log*

# Original merchant photography and video, kept locally so ingestion can be
# re-run. Not committed: it is ~27 MB of originals whose processed output already
# lives in public/media. Keep a backup of this folder somewhere other than this
# machine.
/media-source/

# Capture output, regenerated on demand with SCREENSHOTS=1
/screenshots/
